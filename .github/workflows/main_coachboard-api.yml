name: Azure deployment disabled - coachboard-api

# Workflow desactivado temporalmente.
# Motivo: la cuenta/recurso Azure anterior ya no está disponible.
# Para reactivarlo en el futuro, reconstruir el workflow con el nuevo publish profile
# o con autenticación OIDC hacia la nueva suscripción Azure.

on:
  workflow_dispatch:

jobs:
  deployment-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Azure deployment is disabled
        run: echo "Azure deployment is currently disabled for CoachBoard."
